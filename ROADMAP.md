# Roadmap -- gnu-hurd-docker

This roadmap focuses on making the project **portable**, **truthful**, and **reproducible** across Docker/Podman and amd64/arm64 hosts.

## Milestone 0 -- Baseline integrity (now)

1. [x] Standardize canonical service/container name to `gnu-hurd-dev` (legacy/long-form docs preserved under `docs/**/archive/`)
2. [x] Standardize image reference to `ghcr.io/oichkatzelesfrettschen/gnu-hurd-docker:latest`
3. [x] Ensure `compose.yaml` is TCG-safe by default (no mandatory `/dev/kvm`)
4. [x] Provide explicit `compose.kvm.yaml` for Linux x86_64 acceleration
5. [x] Ensure secrets are optional (no mandatory local secret files for first boot)
6. [x] Keep `./images` as the canonical dev image directory (tracked via `images/.gitkeep`)
7. [x] Make `./scripts/validate-config.sh` the source of truth for internal consistency
8. [x] Add/maintain `./scripts/smoke-host.sh` as the minimal "can I start?" check

## Milestone 1 -- Documentation truth + link hygiene

9. [x] Run a link scanner across `docs/` and fix broken internal links (nested `**/archive/**` excluded)
10. [x] Remove/qualify claims not backed by primary sources (notably hardcoded image dates)
11. [x] Update entry docs (README + docs index + canonical guides) to match current Compose files and scripts
12. [x] Document host/guest architecture clearly (amd64/arm64 host vs x86_64 guest)
13. [x] Document KVM constraints clearly (Linux x86_64-only for x86_64 guests)
14. [x] Document `AUTO_DOWNLOAD_IMAGE=1` (opt-in self-heal)

## Milestone 2 -- Runtime portability (Docker + Podman)

15. [ ] Verify rootless Podman behavior on Linux and document required flags/limitations
16. [x] Add a Docker/Podman invocation path using Compose-native `make` targets + `scripts/lib/container-runtime.sh`
17. [ ] Ensure `docker compose` (v2) vs `podman-compose` differences are documented and tested end-to-end (live sources carry zero v1 `docker-compose` invocations; the remaining 61 live mentions are filenames and prose)
18. [ ] Validate bind mounts and file paths on macOS/Windows via Docker Desktop/Podman Machine (document caveats)

## Milestone 3 -- Reproducible image acquisition

19. [x] Pin Debian image URL to ports/13.0 and verify `SHA256SUMS` (do not hardcode build IDs; example `20250807`)
20. [x] Add checksum verification to `scripts/download-image.sh`
21. [x] Cleanup deterministically on failure and avoid assuming a fixed extracted `.img` filename
22. [x] Document upstream source and verification commands in `docs/audits/`

## Milestone 4 -- CI confidence (without pretending)

23. [ ] CI: validate YAML + shell scripts + internal invariants (fast, no VM boot) -- the workflow definitions exist and `Docs Build Check` runs on pull requests; `validate.yml`, `validate-config.yml`, and `quality-and-security.yml` stay manually disabled until item 43 converges their gates
24. [ ] CI: optional, scheduled "boot smoke" job (best-effort; clearly marked flaky/slow if needed)
25. [ ] CI: publish artifacts for troubleshooting (logs, config dumps)

## Milestone 5 -- Operational ergonomics

26. [x] Add `make` targets (or a small CLI) for: `setup`, `validate`, `up`, `up-kvm`, `up-vnc`, `logs`, `down`
27. [x] Add orchestration commands for VNC/noVNC overlays (`up-vnc`, `up-kvm-vnc`, `up-volume-vnc`)
28. [ ] Add explicit "resource sizing" guidance (RAM/CPU/disk) and sane defaults per host class
29. [ ] Add "known good" troubleshooting playbook for common failures (boot, ssh, disk/fsck, perf)
30. [x] Add a console smoke check (noVNC reachable + monitor "running" + screenshot works)
31. [x] Add an offline GRUB root device patcher (guestfish) so SCSI boots can be tested reproducibly
32. [x] Add disk bus switch docs (`QEMU_DISK_BUS=ide|scsi`) and root device implications (`wd0` vs `sd0`) (SCSI remains experimental for the current image)
33. [ ] Investigate/mitigate KVM+IDE DMA I/O errors; default to TCG for IDE (`AUTO_DISABLE_KVM_FOR_IDE=1`) with `FORCE_KVM=1` override -- blocked on item 35: the 2026-07-24 two-arm capture halts in `fsck` before the guest mounts root read-write, so the condition is not exercised
34. [ ] Investigate guest `sshd` crash on some images; document a known-good image version and/or mitigation steps -- blocked on item 35: no capture has reached multi-user state on the canonical image

## Milestone 5b -- Bootable image integrity

35. [ ] Repair the guest root filesystem in `images/debian-hurd-amd64.qcow2` (`/dev/wd0s5` reports `UNEXPECTED INCONSISTENCY`, `fsck` exits 4, guest stops at a maintenance shell)
36. [ ] Add an offline guest-filesystem gate (`e2fsck -fn` via guestfish) -- `qemu-img check` reports no errors on the same image that fails to boot. `scripts/guestfish-check-guest-filesystem.sh` implements the check with a three-state exit contract (0 clean or repaired, 1 dirty, 2 uncheckable); wiring it into a gate splits by whether an image is present:
    - 36a. [ ] Call the checker from host preflight before QEMU starts, skipping with a recorded reason when the image or guestfish is absent
    - 36b. [ ] Treat exit 1 and exit 2 as failures in any workflow that builds, downloads, repairs, or publishes a qcow2, and install guestfish there. A source-only checkout carries no qcow2, so a green generic run establishes nothing about image integrity
37. [ ] Record SHA256 and upstream provenance for every file in `images/`
38. [ ] Re-run the two-arm KVM/TCG capture past `fsck` once items 35-37 land, and settle items 33 and 34 against evidence

## Milestone 6 -- Packaging coherence

39. [x] Align `PKGBUILD` with runtime realities (host QEMU optional; scripts are arch-independent)
40. [ ] Add a `podman` optional dependency path in packaging docs (where applicable)
41. [ ] Ensure package-installed launcher supports `up-kvm` via `compose.kvm.yaml`

## Milestone 7 -- Gate coverage and reference integrity

42. [x] Discover the maintained shell surface dynamically in `scripts/validate-config.sh` -- `entrypoint.sh`, `scripts/**`, and `share/**`, excluding archives, which replaces 29 enumerated paths with the whole maintained tree. Item 43 names the enumerator that now defines that set; `scripts/list-maintained-shell.sh` is the only authority on its size
43. [ ] Clear the 15 warning-level ShellCheck findings, then raise the enforced severity. `scripts/list-maintained-shell.sh` defines the file set and `scripts/check-maintained-shell.sh` is the single enforcement mechanism, failing closed on an empty surface; `make lint`, `scripts/validate-config.sh`, `validate.yml`, `validate-config.yml`, and `quality-and-security.yml` all run it, with `SHELLCHECK_SEVERITY` selecting the level and `error` enforced. Raising the default to `warning` waits on the findings, because `make lint` has to pass on a clean tree
44. [x] Remove the stale required-script gate from `release-artifacts.yml` and move release acceptance onto the extracted archive rather than repository-source paths, so the gate reads the product it publishes
45. [x] Replace the tab in `.github/workflows/release.yml` (sole yamllint error)
46. [ ] Count recipe blocks, not target lines, in any Makefile duplicate-target check (the five `up-podman*` pairs are the GNU make target-specific variable idiom and are correct)
47. [ ] Reconnect `scripts/test-hurd-system.sh` (and the seven `test-phases/` scripts it reaches) to a Makefile target
48. [ ] Classify the 36 live host-side orphan scripts (41 raw zero-inbound graph nodes, five of which fall outside the live-shell denominator) as operator-tool, guest-scoped, or unreachable-automation in `scripts/INVENTORY.md`
49. [ ] Correct the phantom `QEMU_*` env vars cited in live docs -- the dead `make` targets are corrected and `docs/03-CONFIGURATION/environment-variables.md` names the variables the entrypoint ignores; classifying the remaining live references needs a schema-backed variable inventory rather than a grep
50. [x] Rebuild the `mkdocs.yml` nav against the `01-08` tree, so `mkdocs build --strict` produces `site/index.html`
51. [x] Classify every document as nav, `not_in_nav`, or `exclude_docs`, so adding a report or an audit cannot break the strict build

52a1. [x] Define machine-readable evidence classes and per-probe records. `scripts/capture-runtime-evidence.py` labels every field `observed`, `derived`, `declared`, or `not-captured`, and each probe retains its argv, exit status, and both output streams. `docs/audits/runtime-evidence-capture-protocol.md` carries the contract and `schemas/runtime-evidence-v2.schema.json` the schema
52a2. [ ] Retain both sanitized streams and certify them after redaction. Redaction runs inside `Capture.run` before each stream is written and before its digest is taken, both `stdout_sha256` and `stderr_sha256` are recorded, and JSON is redacted structurally because a regex that consumes a quoted value and writes back an unquoted token produces a document `json_probe` cannot decode. `tests/runtime-evidence/test-capture-producer.py` calibrates the producer against the shapes a real probe returns. A global `core.excludesFile` ignoring `*.out` also kept every retained stdout untracked while its `.err` counterpart committed, so a clone held digests for streams it lacked; `.gitignore` now overrides that rule for the case directories. This closes when a recapture from a clean revision produces the tracked package
52b. [ ] Bind to exactly one QEMU process rather than one container. Container selection by live QEMU process, the Compose service from the `com.docker.compose.service` label, declared configuration read from that service alone, argv from `/proc/<pid>/cmdline`, and image resolution through the container's mount table are in place, and several PIDs in one container exit 2 without `--qemu-pid`. `--image PATH` branches before selection and records the supplied path as declared and the resolved path as observed, so offline image identity no longer depends on a running instance. This closes when a recapture exercises both paths against a live guest
52c1. [x] Execute schema, checker, and producer fixtures in active CI. `make evidence-check` runs both suites and Static Gates runs it. The contract fixtures assert what the document contract rejects: a null value classed `observed`, a null value with a missing or empty reason, a stdout or stderr digest that does not describe its file, a probe advertising an absent stream, a probe recording no stderr digest, a stream path escaping the capture by traversal, an absolute path, a symlink, a directory, a machine-local path, an unredacted credential, a wrong schema version, a missing section, and a directory with no `capture.json`. The producer fixtures assert what the instrument does with a JSON secret member, an environment array, a YAML mapping, a key that merely contains `PASS` or `KEY`, ordinary text, a known and an unknown KVM transcript, and an offline image run that the checker then validates
52c2. [ ] Cover the fixtures that need a live runtime: several QEMU containers, several QEMU processes in one container, named-volume image storage, a custom monitor port, a custom published SSH port, an SSH banner timeout, a missing guest key, and a failed enumerator
52d. [ ] Recapture the matched TCG/KVM pair from a clean identified revision using tracked inputs. `evidence/runtime/cases/SUPERSEDED.md` records why the present pair is not citable: four post-redaction digest mismatches per arm, no stderr digests, a `/tmp` override path that is both machine-local and absent from the tree, and capture from a dirty `b5db85a` that predates the instrument. The pair still shows that changing `FORCE_KVM` from `1` to `0` changed the accelerator with every other captured parameter equal, which is evidence consistent with the `AUTO_DISABLE_KVM_FOR_IDE` branch rather than an observation of it. The recapture follows the single-service Compose topology so the pair is cut once against the topology it describes
52e. [ ] Instrument `entrypoint.sh` to write its accelerator decision as a structured record naming the candidate, each input it tested, the selected accelerator, and a reason code, so a capture reports the reason as an observation of the decision rather than reconstructing it from inputs
52f. [ ] Probe KVM usability rather than device visibility. `test -e/-r/-w /dev/kvm` establishes that the node is visible and its mode bits permit an open, not that `KVM_CREATE_VM` succeeds. `container_kvm_usable` runs a paused diskless `qemu-system-x86_64 -accel kvm` under `timeout`, reads 124 as initialization that survived, a known accelerator diagnostic as failure, and every other outcome as `not-captured` rather than as success. This closes when the probe runs against a live container in both arms of the recapture
53. [x] Define the release source product with `git archive`, so the repository tree and the `.gitattributes` `export-ignore` policy determine its contents, and build it from `scripts/build-release-archive.sh` so the pull-request check and the release job accept the same artifact against the same contract (`make -n up`, `make -n build`, the packaged `scripts/validate-config.sh`, a clean documentation link scan, and the default Compose overlay)
54. [x] Make `compose.minty.yaml` a single-QEMU-service overlay over `compose.yaml` plus `compose.bind.yaml`, naming the canonical `gnu-hurd-dev` service. The overlay declared a service named `hurd` while the base declares `gnu-hurd-dev`, so it added a service rather than overriding one and two QEMU containers contended for the same qcow2; it also embedded the KVM device, `SYS_PTRACE`, `ENABLE_VNC`, the `./images` bind, and host mappings for 2222, 5900, 6080, and 9999 that the base and the VNC overlay already publish. Each concern returns to the overlay that owns it, and the Make targets compose file sets that respect `CONTAINER_RUNTIME`, `COMPOSE`, and `SERVICE_NAME` for `up`, `down`, `ps`, and `logs` alike. `QEMU_SMP` defaults to 1, because the installed kernel is the uniprocessor Mach build and a second vCPU varies host-side QEMU timing rather than guest parallelism. `make topology` renders each composition through the engine and asserts the service set, the guest drive, device ownership, and that no host port is published twice; run against the previous overlay it fails all four compositions, including the 5900 and 6080 collision that would have made a VNC composition fail at container start
55. [x] Resolve the MATE package closure for both Hurd ports from the authenticated archive rather than the guest. `Dockerfile.hurd-archive` pins its base by digest and sets `APT::Architecture` to `hurd-amd64` or `hurd-i386` over a private apt state tree built per invocation, and `make hurd-closure` classifies a set as native, architecture-all, uninstallable, or missing. LMDE 7 `gigi` publishes no `binary-all` directory, so the overlay keeps only its `Architecture: all` stanzas and republishes them per source component under the target architecture, preserving every version, with the overlay origin pinned below Debian Ports. Mint metadata is verified with `gpgv` against a key vendored at `config/keys` and pinned by fingerprint, and each index is accepted only against the SHA-256 in that verified `Release`; a signature failure, an unnamed index, and a hash mismatch each abort. `make hurd-closure-selftest` drives the offline fixture suite in Static Gates, and `evidence/hurd-archive/` holds reports carrying collection time, image digest, apt and dpkg versions, every metadata digest, and the full recursive transaction. The layer settles availability and resolver closure; whether a package installs stays a guest fact
55a. [ ] Build packages in a disposable builder overlay rather than in the canonical image, on the single-service topology 54 established. The mechanism is in place and exercised. `QEMU_BACKING_DRIVE` names an immutable image and `QEMU_DRIVE` an overlay path that must not exist; the entrypoint validates every option that can abort the run before creating anything, records the backing digest, refuses a declared `QEMU_BACKING_SHA256` that disagrees, refuses to reuse an overlay, states the backing format rather than letting qemu-img probe it, and reads the created chain back to confirm it resolves to the declared base. `scripts/run-hurd-build.sh` owns the lifecycle the entrypoint cannot: it execs into QEMU, so no trap of its own survives, and disposal must follow QEMU anyway because artifacts, the filesystem check, and the manifest are read out of the overlay. `compose.builder.yaml` is standalone with `restart: "no"` and no fixed container name, because a restart policy turns a finished build into a loop and a fixed name makes two runs collide. `make overlay-lifecycle` drives eight cases in a real container. What remains is the immutable `hurd-amd64` builder base image named in `config/minty/builder.lock.json`, the unprivileged builder account, the artifact export set, and the offline filesystem check of a finished overlay
55b. [ ] Build `mate-settings-daemon` and `mate-control-center` for `hurd-amd64`, on 55a and in the order 55i establishes. Neither has a 64-bit record while both exist for `hurd-i386` at 1.26.1-1.2 and 1.26.0-2, and the main archive carries `mate-settings-daemon` source at 1.26.1-1.2, the version the i386 binary was built from, which makes it a high-confidence native-rebuild candidate rather than a greenfield port. The same-version argument does not extend to `mate-control-center`: sid carries source 1.26.1-1.1 while the i386 binary is 1.26.0-2. A completed `hurd-amd64` build is the acceptance test for both, because 64-bit assumptions, dependency skew, and new compiler diagnostics can still block a source that builds for the other port
55c. [ ] Port `polkitd`, and `accountsservice` for `hurd-amd64` where it is absent while native on `hurd-i386`. `polkitd` blocks `mate-polkit`, which `mate-panel` pulls, so authorization reaches into the first session rather than staying in the administrative one. A package that `Provides: polkitd` while supplying no daemon persuades apt and authorizes nothing: even on a single-user VM the system distinguishes the desktop account, root, system daemons, operations requiring authentication, and operations that are always denied, and clients then fail over D-Bus at run time. Such a stub counts toward no acceptance profile, which is why `scripts/make-hurd-dependency-stub.sh` was withdrawn rather than kept
55d. [ ] Rebuild `python3-setproctitle` for the Hurd, the one dependency keeping `mintmenu` out of `mint-mate-integration`. Neither port publishes a current binary, and 55i shows the build can start on both: the source is `python-setproctitle` 1.3.7-2 and its build dependencies resolve at 156 packages for each port. The build attempt is the acceptance test
55e. [ ] Watch the `sid` skew between `gvfs` and `gvfs-common` that makes `caja` uninstallable on both ports. Both ports carry `caja` natively, so this is an archive state rather than a port gap: the remedies are convergence, a coherent snapshot where every candidate matches, or rebuilding the mismatched dependency set together against one source state. Manufacturing a local `caja` build because `caja` heads a failing transaction addresses no mechanism
55f. [x] Settle whether `hurd-i386` binaries can fill the 64-bit port's gaps, from the archive rather than by argument. `--foreign-architecture` enables a second architecture the way `dpkg --add-architecture` does and qualifies every request to it. Upstream supports 32-on-64 as a whole 32-bit userland over 64-bit GNU Mach, or a whole 64-bit one, and not both at once, so a mixed universe is unsupported before any measurement; the archive then refuses it in the packaging layer first. `dconf` coinstalls as `:hurd-i386` and pulls 13 foreign-qualified packages, so the mechanism works where packaging is multiarch-aware, and all seven architecture-specific `mate-bootstrap` members refuse because MATE splits each component's data into an `Architecture: all` companion, which cannot satisfy a foreign package's dependency without `Multi-Arch: foreign`, while `mate-settings-daemon` carries no `Multi-Arch` field at all. The set's three `Architecture: all` members stay unqualified and resolve, because such a package is realized once under the native architecture and qualifying one asks for a binary the archive never publishes. Whether a foreign transaction would displace an installed native userland is not measured: the resolver's tree carries an empty dpkg status file, so `native_packages_removed` is empty by construction, and settling replacement against coinstallation needs a tree seeded from the published image's status file. `hurd-i386` is a build reference and a separate reference system, not a package source for the amd64 desktop
55i. [x] Resolve whether each rebuild candidate's build can start, which the binary index does not settle. `--build-dependencies` adds a `deb-src` line for the main archive and resolves each source package's build closure against the target port's binaries, reported by `make hurd-build-closure-report`. The simulation names `source=version`: given a bare name apt binds the source through the port's binary of that name and demands the source at that binary's version, which is why a stale hurd-i386 `python3-setproctitle` 1.1.8-1 binary made a buildable source read as blocked. Of six sources tested for `hurd-amd64`, two are buildable and four are blocked: `libmatemixer` and `python3-setproctitle` resolve 206 and 156 build dependencies and share one builder tree at 240 packages; `mate-settings-daemon` stops at `libmatemixer-dev`, `mate-control-center` at both `libaccountsservice-dev` and `mate-settings-daemon-dev`, `accountsservice` at `polkitd`, and `polkitd`, whose source is `policykit-1`, at `libselinux-dev`. SELinux is a Linux kernel interface, so polkit's blocker is a packaging change qualifying that build dependency to Linux architectures rather than a port of polkit's logic. The work is three partly independent branches rather than one chain: libmatemixer to mate-settings-daemon; python3-setproctitle to the Mint Menu; and the polkit qualification to polkitd to accountsservice, with mate-control-center converging only after both the settings and authorization branches close. Polkit is a build-time blocker rather than only a run-time one. `hurd-i386` differs: `mate-settings-daemon` is already buildable there at 450 build dependencies, and `mate-control-center` is blocked through `mate-polkit`, so the authorization branch gates it on both ports. Two dependency views are recorded because neither contains the other: the declared scan reads the source's own clauses and names a build dependency the port lacks outright, resolving alternatives, architecture restrictions, and virtual providers the way a build daemon would; apt resolves transitively and names a package that exists whose own dependencies do not close. They agree on every `hurd-amd64` candidate, so that build order is the full set rather than apt's first blocker, and they disagree once: on `hurd-i386` every build dependency `mate-control-center` declares exists, while apt still reports `polkitd`, because the blocker is transitive through `mate-polkit`
55j. [ ] Qualify polkit's `libselinux-dev` build dependency to Linux architectures and build `polkitd` for both Hurd ports, in four results rather than one. 55j1 qualifies the build dependency and disables SELinux support where the dependency is unavailable. 55j2 runs the pinned-version build on `hurd-amd64` and `hurd-i386`, which is where Linux-specific headers, process-credential introspection, session tracking, and local-socket peer credentials surface. 55j3 exercises `polkitd` over the Hurd system bus and process-identity mechanism, because a produced `.deb` says nothing about the daemon answering. 55j4 builds `accountsservice` on the runtime contract rather than on the build alone. `libselinux-dev` is the first barrier, not evidence that polkit needs no portability work. This is the packaging half of 55c; a Hurd-compatible authorization service or a deliberate Hurd-native replacement is the other half, and neither is a stub
55g. [ ] Run the guest half of the multiarch experiment in a disposable clone or external overlay, never in the canonical image. Simulate before installing and abort on removal of Essential or Important `hurd-amd64` packages, replacement of native Hurd servers, unqualified `/usr/bin` or `/usr/sbin` collisions, unresolved pre-dependencies, mixed-version libc or libpthread constraints, or a large unexpected foreign closure. If a minimal closure is safe, install the foreign libc and one uncomplicated executable rather than MATE, and drive a graded probe: pure computation, standard streams, `getuid` and `uname`, filesystem operations, `fork` and `exec`, Unix-domain sockets, networking, Mach port operations, translator-backed filesystem access, D-Bus session communication, and a GTK window. A working `hello:hurd-i386` establishes that some 32-bit processes run inside the amd64 system and nothing about product acceptance
55h. [ ] Build `minty-hurd-i386.qcow2` as a behavioral reference product beside the amd64 primary, using the more complete 32-bit package set to answer sooner whether MATE starts when the archive carries the missing binaries, which remaining failures are architecture-independent, whether the D-Bus and GSettings session design works, whether the Mint visual family installs and persists, whether `polkitd` is still the blocker, and whether the panel layout and theme configuration work. It is a reference system rather than a shipped desktop, and it keeps those answers out of the amd64 image rather than contaminating it with an unsupported mixed userland
56. [ ] Install the MATE session core and add `minty-hurd-mate`, keeping XFCE as an explicitly selected rescue session. The install names the transaction the archive accepts today, which is the resolvable subset of `mate-bootstrap`: `mate-session-manager`, `marco`, `mate-terminal`, `mate-notification-daemon`, `mate-menus`, `mate-icon-theme`, and `mate-themes`. `mate-panel` stays out because it declares `mate-polkit` as a hard `Depends`, so omitting the authorization agent from a request does not keep it out of the transaction and the panel is gated on 55j alongside the settings-administration layer. Privileged configuration uses explicit root commands and the administrative GUI actions are recorded as unavailable rather than silently broken. Adding the panel earlier takes a packaging change making the agent optional, a substitute panel recorded as a deviation, or a genuine alternative polkit service, and a `Provides: polkitd` stub is none of those. The wrapper resolves the session binary from the installed package rather than assuming a name, and sets the desktop identity MATE components read. `/etc/hurd-desktop.session` selects the session and `/etc/hurd-desktop.mode` the Xorg or VNC route
57. [ ] Narrow the D-Bus session before MATE becomes the default. The current fix permits ANONYMOUS authentication and modifies the system bus; MATE depends on the session bus and GSettings for theme, panel, and window-manager state, so the session moves to a per-user loopback bus with `DBUS_COOKIE_SHA1` and no anonymous system bus
58. [ ] Install every dependency-safe LMDE Mint visual package unchanged under its own name, and fail the installer when the declared theme manifest silently shrinks. The 35 members of `mint-visual` dependency-resolve for both ports, which is a resolver verdict rather than an installation: an `Architecture: all` package can still carry a maintainer script calling a Linux-only command, Python reading `/proc`, or systemd activation, so the claim closes on a disposable guest installation ending `dpkg -C` clean. The set is recorded as a version-locked manifest with each package's component, digests, and reason for inclusion rather than as a hardcoded list. The default theme comes from an ordered probe over the themes actually installed rather than a name asserted before the packages are examined
59. [ ] Make MATE the default session in both Xorg and VNC modes, gated on process and settings evidence: `mate-session`, `marco`, `mate-settings-daemon`, and `mate-panel` running, Caja owning the desktop, `mate-terminal` executing a command, GSettings persisting across reboot, the selected Mint themes active, and no XFCE process running
60. [ ] Integrate Mint Menu when its simulated dependency closure carries only LMDE architecture-independent payloads and native `hurd-amd64` MATE and XApp libraries, falling back to the MATE menu. `python3-setproctitle` is rebuilt first and its extension module is exercised under the guest's own Python version before the closure is reevaluated, because a module that builds and does not import blocks the menu just as completely. Whisker Menu stays specific to the XFCE rescue session
62. [x] Resolve the native development profile from the archive, which is the closure half of the compiler requirement and needs no guest. `make hurd-closure-report HURD_SET=dev-profile` classifies the compiler, build-system, packaging, and Hurd development packages against the port. All 31 members resolve for `hurd-amd64`, 23 native and 8 architecture-independent, pulling 261 packages; `mig-x86-64-gnu` is present at 1.8+git20231217-11, and `git` carries a Hurd-specific revision at 1:2.53.0-1+hurd.1. `hurd-i386` resolves 30 of 31, missing only the amd64 cross-generator. A resolved set is availability rather than capability, so whether each compiler compiles, links, and runs is 63
63. [ ] Exercise the native compiler suite inside the guest: C and C++ compile, link, and execute; a shared library through `dlopen`; a static archive; pthread creation and synchronization; a socket client and server; atomics; large-file I/O; and an Autoconf, a CMake, and a Meson project each configuring and building. GDB runs a program and produces a backtrace. Every result is recorded per language rather than as one pass
64. [ ] Probe the Mach and Hurd development path, which is what distinguishes this from a generic Linux toolchain: MIG generates client and server stubs, the generated stubs compile, a small Mach IPC program executes, a translator or Hurd-server probe builds, and a bounded GNU Mach or Hurd source target configures and completes
65. [ ] Probe the optional compiler matrix rather than assuming it: Clang with LLVM and lld, gfortran, GNAT, Objective-C, Rust with Cargo, Go, TCC, and Java. An unavailable compiler is recorded unavailable; an installed one is advertised only after a native compile, link, and run probe succeeds
66. [ ] Publish a toolchain manifest naming each compiler, its package version, its target triple, the architecture, and its compile, link, and execute results with the evidence path, so the development environment is a stated contract rather than a package inventory
61. [ ] Build `minty-emerald-mate-theme` as a separate package deriving its palette from CachyOS-Emerald-KDE and covering GTK, Marco decorations, panel, Caja, control center, terminal, notifications, and wallpapers. It leaves the Mint families installed and selectable rather than overwriting them


67. [ ] Seed the closure resolver with the guest's own installed-package state. The two mechanisms are in place: `scripts/export-guest-package-state.sh` renders RFC822 paragraphs through `dpkg-query` rather than a table, because apt reads installed state as paragraphs and a column layout is not an input it can take, and it spells `Status` from its three parts because apt parses `install ok installed` and not `ii`; `--installed-status` copies that file into the tree's `Dir::State::status`, refuses a file naming no package, refuses one carrying the other port's architecture because that would make every native verdict wrong while still parsing, and records kind, architecture, package count, and digest in provenance. A fixture shows the difference the seed makes: the same transaction reports removing an installed package against a seeded baseline and removes nothing against an empty one. What remains is the boot that produces the file, which runs against a throwaway overlay of `hurd-working.qcow2` and never opens the canonical image for writing
68. [ ] Record the accelerator decision as a runtime evidence stream rather than only as a file in the container, so a matched TCG and KVM pair closes 52a2, 52b, 52d, 52e, and 52f from one boot each. The entrypoint already writes the candidate, the demotion inputs, the selection, and a reason code before it execs, so the reason is observed rather than reconstructed from log prose and a QEMU argv
69. [ ] Qualify KVM as the Minty default using the package builder as the workload rather than a separate phase. The entrypoint demotes KVM on `pc` with IDE because of a bus-master DMA fault under sustained writes, and `dpkg-buildpackage` across a 240-package build tree is precisely that workload, so the first build that completes under `FORCE_KVM=1` with a clean offline `e2fsck` is the qualification. Until it does, the builder runs TCG

70. [x] Pin every build-scheduling closure to one archive state with `--archive-snapshot`, forwarded through every report target as `HURD_ARCHIVE_SNAPSHOT`. sid and unreleased move, so an unpinned closure and the build it schedules can resolve against different archives. A snapshot Release also carries the `Valid-Until` it was published with, so the pinned state stops being acceptable to apt weeks later while the bytes and the signature are unchanged; expiry checking is therefore disabled for snapshot sources and left enabled for live mirrors, where a stale Release is a real freshness failure. A fixture backdates a Release and asserts both halves. The lock has a second bound the timestamp does not express: debian-ports rotates its archive signing key, so a January 2026 snapshot fails signature verification with a missing key rather than expiry, and a timestamp is usable only with a keyring vintage that can verify it. `config/minty/builder.lock.json` carries the timestamp, the resolver image, the base digest, and the exact source versions, so the resolver, the guest APT configuration, and the builder read one file
71. [ ] Publish the built packages as a signed local repository under `artifacts/apt/` and let the closure resolver read it as another authenticated input, recording its Release digest and signing-key fingerprint. Without that the dependency graph is documented and not machine-executable: nothing demonstrates that a new `libmatemixer-dev` makes the `mate-settings-daemon` build closure resolve, or that a new `polkitd` makes `accountsservice` resolve, short of attempting each build

72. [ ] Retire the legacy guest installers rather than extending them, against a defect inventory rather than an impression. `scripts/minty-hurd-install.sh` sets `set +e`, wraps every install in `apt-cache show` so an unavailable package prints `skip-missing` and continues, pipes `apt-get` through `tail` so even a present package's exit status is discarded, and ends by declaring success: it skips `x11vnc`, which `scripts/hurd-desktop-autostart.sh` then invokes at boot to export port 5901, so the advertised access path depends on a package the installer reported skipping. `scripts/lmde7-apt-setup.sh` fetches the LMDE keyring over plain HTTP with no pinned digest and installs it with `dpkg -i`, which runs maintainer scripts as root, making the trust anchor itself unauthenticated; it also writes the keyring into `/etc/apt/trusted.gpg.d/`, where it becomes a valid signer for the debian-ports lines that carry no `signed-by`. `scripts/provision-hurd-x11.sh` disables host-key verification on the channel that pipes root heredocs into the guest, passes the root password through `sshpass -p` where `ps` can read it, and grants the `agents` account passwordless sudo. Under `/bin/sh` there is no `pipefail`, so `... | tail -N` masks a failed `apt-get update` and the next step installs from a stale index. The replacement provisioner carries required, optional, and forbidden package manifests, a phase journal, the archive lock from 70, idempotency, failure propagation, and a post-phase acceptance check; the current scripts move under an archive path because the published image was built with them
73. [ ] Decide and record the Mach IPC posture before the first builder run. The tree already carries a GNU Mach patch raising `ipc_kernel_map_size` from 8 MiB to 64 MiB after fork-heavy startup produced repeated allocation failures, and a 240-package dependency installation followed by compiler-heavy builds is that workload again. Either install the dependency set in bounded batches with a resume journal on a stock kernel, or build a reproducible patched kernel package and keep the stock kernel as a GRUB fallback. Discovering the limit partway through a multi-hour build with no resume journal is the outcome this item exists to prevent
74. [ ] Freeze the Mint visual set as build input rather than as a resolver verdict. Debian is pinned through snapshot.debian.org and LMDE is not, so the image builder downloads every selected `.deb`, verifies each payload SHA-256 against the authenticated Packages index, retains a version, component, and digest manifest, republishes the exact payloads in the signed local repository, inspects maintainer scripts and payload paths, installs into a disposable product overlay, and requires `dpkg -C` clean. Only then is "the Mint themes install on Hurd" observed rather than resolved
75. [ ] Split the published artifact from the product target in the public documentation. The README still calls the guest Debian 13 Trixie while it is Debian Ports `forky/sid` plus `unreleased`, states a 6.7 GiB expansion against a 16 GiB virtual disk, and presents XFCE, Whisker Menu, and TightVNC as the Minty design. The current downloadable artifact is Minty Hurd XFCE and the target is Minty Hurd MATE; the XFCE walkthrough becomes a historical document under its own name so a new reader does not run the historical installer expecting the target system

76. [ ] Replace the fixed VNC credential and the sleep-based readiness chain in the desktop launcher. `scripts/hurd-desktop-autostart.sh` stores `hurdhurd` when no password file exists, so the guard preserves a constant that README and MINTY-HURD-README both publish rather than a secret, passes it through `x11vnc -storepasswd` where `ps` can read it, and runs `-forever -shared` with no `-localhost`, so the server binds every guest interface behind a host bind that is a variable. The launcher the script generates carries no `set -e`, backgrounds its whole mode block, and exits 0, so a missing Xvfb, dbus, session binary, or x11vnc produces a guest that boots reporting success with no desktop. `sleep 8` and `sleep 5` stand in for `xdpyinfo` and a session-alive probe, and `sleep 1` stands in for the `dbus-send` call the same script prints as the manual verification step. It also deletes `/tmp/.X1-lock` on every boot without checking for a live session, so a second invocation spawns duplicate Xvfb, session, and x11vnc processes contending for 5901
77. [ ] Repair the GHCR publication gate and reconcile what `compose.yaml` pulls. The multi-platform build at `push-ghcr.yml:62-75` sets both `push: false` and `load: false`, so no image reaches the local engine, and the test step's first command `docker images | grep gnu-hurd` exits 1 under the runner's `bash -eo pipefail` and terminates the job before the push step runs. The regression landed 2026-02-24 in b3721ca, which changed `platforms: linux/amd64` with `load: true` to two platforms with `load: false`; eight consecutive push runs since fail at that step with the push skipped. The published tag is therefore live and frozen at 2026-01-11, which is worse than absent: a bare pull supplies a stale image rather than failing. Split it the way `create-release.yml` still does, an amd64 `load: true` build for the smoke test and the existing multi-arch `push: true` step for publication, assert on the build step's `imageid` rather than grepping `docker images`, and either wire up or delete the `image-multiarch` and `ghcr` bake targets, which no workflow, target, or script invokes
78. [ ] Correct the user-facing documentation against the artifact rather than the intent. The README called the guest Debian 13 Trixie in three places, advertised an `agents`/`agents` account with passwordless sudo as a shipped default when the released image carries `root`/`root` and `user`/`user` both expired at first login, and pinned script and document counts that had drifted from 21 to 84 and 26 to 225; those are corrected. The SMP claims are corrected too: `docs/02-ARCHITECTURE/SYSTEM-DESIGN.md` stated that Hurd SMP is stable and production-ready and recommended two or more cores, which the uniprocessor `GNU-Mach 1.8+git20260224-up-amd64` build falsifies, and that section now records `nproc` as the falsifier for any future change. What remains is the product split: `MINTY-HURD-README.md` presents XFCE, Whisker Menu, and a KVM default as the Minty design when KVM is demoted on `pc` with IDE unless forced and MATE is the product target. The published artifact and the target need separate documents so a reader does not run the historical installer expecting the target system

## Definition of done (per change)

- The change is reflected in:
  - `README.md` (if user-facing)
  - `docs/` (if it alters behavior or requirements)
  - `scripts/validate-config.sh` (if it alters invariants)
- `./scripts/smoke-host.sh` passes on a clean checkout.
