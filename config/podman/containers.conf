[engine]
# Pin the compose provider so `podman compose` does not delegate to an
# unexpected docker-compose binary.
compose_providers = ["/usr/bin/podman-compose", "podman-compose"]
compose_warning_logs = false
