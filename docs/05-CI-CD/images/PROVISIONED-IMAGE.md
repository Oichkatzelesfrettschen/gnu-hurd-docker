# Provisioned Image (Current)

This repository currently supports a reproducible “download + convert” flow via `scripts/setup-hurd-amd64.sh` and does not ship a maintained, versioned pre-provisioned QCOW2 artifact as part of the canonical workflow.

Historical notes about “provisioned images” are preserved at `docs/05-CI-CD/images/archive/PROVISIONED-IMAGE-LEGACY.md`.

